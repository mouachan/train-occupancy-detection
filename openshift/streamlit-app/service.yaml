apiVersion: v1
kind: Service
metadata:
  name: train-detection-streamlit
  labels:
    app: train-detection-streamlit
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 8501
    targetPort: 8501
    protocol: TCP
    name: http
  selector:
    app: train-detection-streamlit
